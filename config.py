# =============================================================================
# config.py — Application Configuration & Environment Settings
# =============================================================================
# Centralizes all configuration values for the application including:
#   - Loading environment variables (API keys, model names)
#   - App-wide constants (supported languages, difficulty levels, etc.)
#   - Streamlit page configuration defaults
#
# All sensitive credentials must be stored in a .env file (never committed).
# =============================================================================

# TODO: Load environment variables and define app-wide constants
